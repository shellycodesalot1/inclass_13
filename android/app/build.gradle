plugins {
    id "com.android.application"
    id 'com.google.gms.google-services'
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.inclass_13"
    compileSdk = 34  // Update to 34 for Firebase compatibility

    ndkVersion = "23.1.7779620"  // Set specific NDK version if required by your project

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.inclass_13"
        minSdk = 23  // Minimum SDK version for compatibility
        targetSdk = 34  // Updated to 34 for Firebase compatibility
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug  // Use a proper signing config for production releases
        }
    }
}

flutter {
    source = "../.."
}
